

import os
# datetime 모듈에서 datetime 클래스 불러와
from datetime import datetime
# datetime.now()

# random 모듈에서 random 클래스 불러와
from random import random
# random.now()


#log 디렉토리 없으면 log 디렉토리 생성한다
if not os.path.isdir('log'):
    os.mkdir('log')

else:
    if not os.path.isfile('log/count_log.txt')
        with open('log/count_log.txt', 'a', encoding='utf8') as fi:
            for i in range(1, 11):
                stamp =
                value =
                pass
            df